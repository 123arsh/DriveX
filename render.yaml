services:
  - type: web
    name: drivex-backend
    env: node
    plan: free
    repo: https://github.com/123arsh/DriveX
    branch: main
    rootDir: backend
    buildCommand: npm ci --production
    startCommand: npm start
    autoDeploy: true
