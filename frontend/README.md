# DriveX Frontend

This application contains the customer-facing React frontend for DriveX.

## Run locally
1. Install dependencies: `npm install`
2. Start development server: `npm run dev`

## Notes
- Built with Vite, React, Tailwind, Framer Motion, and GSAP
- Uses a modular route structure and context-based theme provider
