# DriveX Backend

This application contains the DriveX REST API and business logic.

## Run locally
1. Copy `.env.example` to `.env`
2. Install dependencies: `npm install`
3. Start development server: `npm run dev`

## Notes
- Built with Express, MongoDB Atlas, JWT, Cloudinary, Razorpay, and Swagger
